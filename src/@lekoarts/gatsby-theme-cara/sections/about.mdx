## Projects

<div className="project" style={{marginBottom: '40px', background: 'linear-gradient(to right, #D585FF 0%, #00FFEE 100%)', padding: '20px', borderRadius: '8px'}}>
  <h2 style={{marginTop: '20px', color: '#333'}}>Revamped Stride Scheduling in xv6<span style={{float: 'right', fontStyle: 'italic', color: '#666'}}>C, QEMU</span></h2>
  <p>Implemented lottery scheduling, ticket mechanism, and stride scheduling for equitable CPU resource distribution. Enhanced system efficiency by dynamically calculating stride values and intelligently selecting the next processes based on pass values.</p>
</div>

<div className="project" style={{marginBottom: '40px', background: 'linear-gradient(to right, #009245 0%, #FCEE21 100%)', padding: '20px', borderRadius: '8px'}}>
  <h2 style={{marginTop: '20px', color: '#fff'}}>Out of Order CPU Simulation <span style={{float: 'right', fontStyle: 'italic', color: '#ccc'}}>Embedded C</span></h2>
  <p>Developed an out-of-order CPU simulation with 5 stage pipeline and extended it to include Superscalar features. The simulated CPU supported register renaming, Reorder Buffer, Reservation stations, Prediction Table, and a variety of Instruction Set Architecture (ISA) instructions, such as ADD, ADDL, LOAD, STORE, and BZ etc.</p>
</div>

<div className="project" style={{marginBottom: '40px', background: 'linear-gradient(to right, #662D8C 0%, #ED1E79 100%)', padding: '20px', borderRadius: '8px'}}>
  <h2 style={{marginTop: '20px', color: '#fff'}}>Pneumonia Detection Using Chest X-ray (CXR) images. <span style={{float: 'right', fontStyle: 'italic', color: '#ccc'}}>TensorFlow</span></h2>
  <p>Applied an innovative pneumonia detection method using Convolutional Neural Networks (CNN) and Principal Component Analysis (PCA) for feature extraction from CXR images. Achieved a notable accuracy boost, reaching 85% in classifying pneumonia cases.</p>
</div>

<div className="project" style={{marginBottom: '40px', background: 'linear-gradient(to right, #D4145A 0%, #FBB03B 100%)', padding: '20px', borderRadius: '8px'}}>
  <h2 style={{marginTop: '20px', color: '#fff'}}>Gaussian Naive Bayes Classifier for Multi-Class Data <span style={{float: 'right', fontStyle: 'italic', color: '#ccc'}}>Python</span></h2>
  <p>Data was split into a secure 60:40 ratio for training and validation, and 5-fold validation was performed. A Gaussian Naive Bayes model was implemented from scratch and trained on the training data. The model was then tested on the test data, achieving an accuracy of 87%.</p>
</div>
